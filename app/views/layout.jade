doctype 5
html
  head
    title="Ateliês"
    link(rel='stylesheet', href='/stylesheets/style.css')
    block head
  body
    block layout
    block scriptsFooter
<!--App #{CONFIG.app.name} version #{CONFIG.app.version} running on node #{process.version} on environment #{CONFIG.serverEnvironment} -->
