append variables
  - title += "Administração do site"
extends homeLayout
append scriptsFooter
  script.
    var siteAdminBootstrapModel = {
      user:!{JSON.stringify(user)}
    }
  script(src='#{CLIENT_LIB_PATH}/lib/requirejs/require.js', data-main='#{CLIENT_LIB_PATH}/bootstrap.js')
  script.
    require(['siteAdminBootstrap']);

append content
  .container.siteAdminContainer
    section#app-container
      .siteAdmin Carregando...
