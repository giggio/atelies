block variables
  - var title = "Ateliês - "
  - var pageSchema = "http://schema.org/WebPage"
  - var dontShowLoginPopup = false
doctype html
html
  head
    title=title
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    link(rel='stylesheet', href='#{STATIC_PATH}/stylesheets/style.css')
    link(href="https://plus.google.com/111286901107324902673", rel="publisher")
    include includes/shims
    script(type='text/javascript').
      window.prerenderReady = false;
      var staticPath = '!{STATIC_PATH}';
      var clientLibPath = '!{CLIENT_LIB_PATH}'; //not used anywhere, but is here so we remember clientLibPath and staticPath are different
    block head
  body(itemscope, itemtype=pageSchema)
    #overlay: #loading
    block layout
    block scriptsFooter
      if DEBUG
        script(type='text/javascript').
          window.DEBUG = true
      else
        script(type='text/javascript').
          window.DEBUG = false
      script(type='text/javascript').
      <!-- UserVoice JavaScript SDK (only needed once on a page) -->
      script(type='text/javascript').
        (function(){var uv=document.createElement('script');uv.type='text/javascript';uv.async=true;uv.src='//widget.uservoice.com/jNBpyNyNkKBnCxUMJjJziw.js';var s=document.getElementsByTagName('script')[0];s.parentNode.insertBefore(uv,s)})()</script>
      script(type='text/javascript').
        UserVoice = window.UserVoice || [];
        UserVoice.push(['showTab', 'classic_widget', {
          mode: 'full',
          primary_color: '#cc6d00',
          link_color: '#007dbf',
          default_mode: 'feedback',
          forum_id: 216427,
          tab_label: 'Feedback',
          tab_color: '#cc6d00',
          tab_position: 'middle-right',
          tab_inverted: false
        }]);
<!--App #{CONFIG.app.name} version #{CONFIG.app.version} running on node #{process.version} on environment #{CONFIG.serverEnvironment} -->
